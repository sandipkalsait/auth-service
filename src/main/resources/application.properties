spring.application.name=auth-service
server.port=8081

server.servlet.context-path=/auth

spring.datasource.url=jdbc:postgresql://localhost:5432/warehouse
spring.datasource.username=sandip.kalsait
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret.key=84jkdjcbljdbljf438ur983eddjsjdKJHDSKj
jwt.secret.expiration=10

# Define the log file name and path
logging.file.name=logs/${spring.application.name}.log

# Define the logging level (optional, set the desired log level)
logging.level.root=INFO
logging.level.com.ps.auth_service=DEBUG

# Define log file pattern (optional)
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Define maximum log file size and backup logs (optional)
logging.file.max-size=10MB
logging.file.max-history=30